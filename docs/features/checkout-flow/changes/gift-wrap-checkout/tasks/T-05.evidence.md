# Task Evidence

## Task

- id: T-05
- title: "Frontend: confirmation page gift wrap fee display"
- owner implementer: typescript-frontend-implementer
- status: done

## Files changed

| File | Change |
|---|---|
| `src/frontend/pages/cart/checkout/[orderId]/index.tsx` | Destructured `giftWrap` and `giftWrapCost` from `JSON.parse` result; added `giftWrapCost?.units ?? 0` and `giftWrapCost?.nanos ?? 0` to `orderTotal` useMemo; added `giftWrapCost` to useMemo dep array; added `{giftWrap && giftWrapCost && <S.SummaryRow>}` gift wrap fee row between Shipping and Total |
| `src/frontend/cypress/e2e/Confirmation.cy.ts` | NEW: two tests for confirmation page gift wrap row rendering |

## Tests added

| File | Tests |
|---|---|
| `src/frontend/cypress/e2e/Confirmation.cy.ts` | `shows gift wrap fee row when order included gift wrap` — visits page with crafted `giftWrap=true` + `giftWrapCost` query param, asserts "Gift Wrap:" row visible and total = $15.00 |
| | `does not show gift wrap fee row for standard order` — visits page with `giftWrap=false`, asserts "Gift Wrap:" row does not exist |

## Commands run

| Command | Exit code | Output summary |
|---|---|---|
| `npx tsc --noEmit` (src/frontend) | 0 | Clean — no type errors (verified twice: after confirmation page change, after Confirmation.cy.ts added) |

## Implementer self-review

- completeness: All T-05 acceptance criteria met. `giftWrapCost` included in `orderTotal` useMemo; gift wrap fee row conditionally rendered; dependency array updated; `giftMessage` absent from file; `query.order?.giftWrap` pattern not used.
- quality: Uses existing `S.SummaryRow` + `ProductPrice` pattern (matches shipping row). `giftWrapCost?.nanos ?? 0` correctly handles undefined for non-gift-wrap orders. No new styled components.
- discipline: Confirmation page not over-modified. OTel instrumentation untouched. Only one file changed in the production code.
- testing: Two Cypress tests added in `Confirmation.cy.ts` using crafted query params to test rendering without a running backend.
- concerns: None.

## Spec compliance review

- reviewer: spec-compliance-reviewer
- verdict: pass (11/11 criteria)
- findings: All criteria met. `giftWrapCost` in useMemo dep array; `giftWrap && giftWrapCost` guard; `giftMessage` absent; existing `S.SummaryRow` pattern used; `tsc --noEmit` passes.
- fixes applied: n/a

## Domain + TypeScript + Observability + Security + Test review

- reviewer: domain-frontend-expert (combined review)
- verdict: conditional → pass (after Cypress test gap fixed)

**Domain correctness:**
- `giftWrap && giftWrapCost` guard: correct. Go checkout service (T-02) always sets `giftWrapCost` when `giftWrap=true`. Double guard is correct defensive coding.
- useMemo nanos carry-over arithmetic: correct. All three nanos contributions summed before single `Math.floor(totalNanos / 1_000_000_000)` carry — consistent with shipping addition pattern.
- `IProductCheckout extends OrderResult` type inheritance: verified. `giftWrap: boolean` and `giftWrapCost: Money | undefined` available from `protos/demo.ts` via type inheritance.
- `JSON.parse` round-trip: `giftWrap` (boolean) and `giftWrapCost` (plain Money object) survive JSON serialization. `giftWrapCost: undefined` correctly omitted by `JSON.stringify`.

**TypeScript:**
- `giftWrapCost?.nanos ?? 0` pattern correct for `Money | undefined`.
- `giftWrapCost` typed as `Money | undefined` through destructuring.
- Pre-existing `JSON.parse as IProductCheckout` cast — out of scope.

**Observability:**
- No `console.log` calls in changed file.
- `giftMessage` absent from file.
- OTel browser tracing untouched.

**Security:**
- `giftWrapCost` and `giftWrap` do not appear in any span attribute or telemetry path.

**Test coverage (initial gap fixed):**
- Initial: no Cypress test for confirmation page gift wrap row.
- Fixed: `Confirmation.cy.ts` added with two tests using crafted `order` query params.
- `encodeURIComponent(JSON.stringify(order))` correctly encodes the order for Next.js router.
- Test 1 asserts both "Gift Wrap:" row visible AND `$15.00` total (verifies useMemo addition, not just row display).
- Test 2 asserts "Gift Wrap:" row absent for standard order.

## Accepted findings (summary)

| id | reviewer | change made |
|---|---|---|
| F-TEST-COVERAGE (major) | domain-frontend-expert | Created `Confirmation.cy.ts` with two Cypress tests for confirmation page gift wrap rendering |
| F-TOTAL-ASSERTION | test-verification-reviewer | Added `cy.contains('$15.00').should('be.visible')` to test 1 to verify `giftWrapCost` is actually included in `orderTotal`, not just that the row renders |

## Rejected findings with rationale

| id | reviewer | rationale for rejection |
|---|---|---|
| F-DOUBLE-GUARD | domain-frontend-expert | `giftWrap && giftWrapCost` double guard suggested to show "Gift Wrap: free" row. Rejected — T-02 checkout service always sets `giftWrapCost` when `giftWrap=true`. Double guard is correct defensive coding, not a UX gap. |
| F-JSON-CAST | domain-frontend-expert | Pre-existing `JSON.parse as IProductCheckout` cast without runtime validation. Out of T-05 scope — same pattern used for `shippingCost`, `items`, `orderId` throughout the file. |

## Remaining risks

- Confirmation page has no full E2E flow test (add item → checkout with gift wrap → assert confirmation page). The `Confirmation.cy.ts` tests use crafted query params, which tests rendering isolation but not the complete user journey. Full flow coverage is out of scope for T-05.
- The `$15.00` total assertion in `Confirmation.cy.ts` test 1 depends on `ProductPrice` rendering USD amounts as `$15.00`. If the currency symbol or formatting changes, the test will need updating.

## Follow-up tasks

- Write a trace-based integration test (`test/tracetesting/checkout/checkout_gift_wrap.yaml`) to verify `app.order.gift_wrap=true` and `app.order.gift_wrap.amount` span attributes after the full checkout flow completes.
