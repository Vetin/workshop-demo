# Task Evidence

## Task

- id: T-03
- title: Implement gift message rendering in email service (Ruby)
- owner implementer: ruby-implementer
- status: done

## Files changed

| File | Change |
|---|---|
| `src/email/email_server.rb` | Added `require "cgi"` at line 4; updated `erb(:confirmation, ...)` call to pass `gift_message: data.gift_message` |
| `src/email/views/confirmation.erb` | Added conditional gift message block after `</table>` with nil+empty guard and `CGI.escapeHTML(gift_message.to_s)` |
| `src/email/email_server_test.rb` | New file: 8 tests covering gift message rendering, absence, empty string, XSS escaping, null JSON, PII span guard, pony exception safety, and grace handling |
| `src/email/Gemfile` | Added `ruby "~> 3.4"` directive; added `group :test` with `rack-test` and `minitest` |
| `src/email/Gemfile.lock` | Updated via `bundle install` |

## Tests added or updated

| File | Tests |
|---|---|
| `src/email/email_server_test.rb` | `test_gift_message_rendered_when_present` |
| | `test_no_gift_message_section_when_absent` |
| | `test_gift_message_section_empty_string` |
| | `test_gift_message_xss_escaped` |
| | `test_no_gift_message_key_handled_gracefully` |
| | `test_pony_exception_does_not_capture_gift_message` |
| | `test_gift_message_null_in_json` |
| | `test_gift_message_not_in_span_attributes` |

## Commands run

| Command | Exit code | Output summary |
|---|---|---|
| `ruby -c email_server.rb` | 0 | `Syntax OK` |
| `bundle install` | 0 | Bundle complete, 95 gems |
| `bundle exec ruby email_server_test.rb` | 0 | `8 runs, 25 assertions, 0 failures, 0 errors, 0 skips` |

## Implementer self-review

- completeness: All T-03 acceptance criteria met. `require 'cgi'` present. `erb` call passes `gift_message`. ERB guard is nil-then-empty, in correct order. `CGI.escapeHTML(gift_message.to_s)` used. `send_email(data)` signature unchanged.
- quality: Follows existing Sinatra patterns. CGI stdlib used correctly. Test stubs are comprehensive.
- discipline: TDD followed — tests written alongside implementation. Tests verified passing locally.
- testing: 8 tests cover all acceptance criteria including XSS guard, PII span attribute check, null JSON, and Pony exception path.
- concerns: `OpenTelemetry.logger_provider` is not explicitly stubbed; relies on OTel SDK no-op proxy. This is safe but implicit.

## Spec compliance review

- reviewer: spec-compliance-reviewer
- verdict: conditional → pass (after SC-11 tests added)
- findings:
  - SC-01 through SC-09: all PASS
  - SC-10 (INFO): `gift_message.to_s` defensive coercion — ACCEPTED AND APPLIED
  - SC-11 (BLOCKER): Tests 5 and 6 from task spec missing (`test_nil_order_gift_wrap_handled_gracefully`, `test_pony_exception_does_not_capture_gift_message`) — ACCEPTED AND FIXED
- fixes applied: All 6 task-spec tests now present (plus 2 additional: null JSON, span attribute PII guard)

## Domain expert review

- reviewers: domain-email-expert
- verdict: pass
- findings:
  - `data.gift_message` correct for OpenStruct — PASS
  - nil guard order correct — PASS
  - `CGI.escapeHTML` available after `require 'cgi'` — PASS
  - `emailMemoryLeak` feature flag path preserved — PASS
  - Minor: logger_provider not stubbed (relies on SDK no-op) — noted, not blocking
  - Minor: test payloads use `items: []` — items rendering not exercised, not a T-03 concern
- fixes applied: n/a

## Technical review

- reviewers: technical-ruby-reviewer
- verdict: conditional → pass (after accepted fixes)
- findings:
  - F-7.2 (Minor): Missing `ruby` directive in Gemfile — ACCEPTED AND FIXED (`ruby "~> 3.4"`)
  - F-8.4 (Moderate): `assert !x.nil?` masks server crashes — ACCEPTED AND FIXED (`refute_nil`, `set :show_exceptions, false`)
  - F-8.3 (Minor): No test for explicit JSON null — ACCEPTED AND FIXED (test added)
  - F-1.2 (Minor): `nil? || empty?` idiom — REJECTED: task spec explicitly prescribes this guard order as a documented safety requirement
  - F-1.1 (Minor): `frozen_string_literal` — REJECTED: pre-existing gap across entire service, not T-03 introduced
  - F-5.2 (Info): Dead `alias_method` — REJECTED: INFO-only, no correctness impact
- fixes applied: Gemfile ruby directive, `refute_nil`, `show_exceptions: false`, null JSON test

## Observability review

- reviewer: observability-reviewer
- verdict: pass
- findings: All PASS — no gift_message in any span attribute, `app.email.recipient` preserved, `app.order.id` preserved, `app.confirmation.counter` unchanged, OTel SDK init unchanged
- fixes applied: n/a

## Security / data leak review

- reviewer: security-data-leak-reviewer (combined with observability in single review)
- verdict: pass
- findings:
  - gift_message not in any puts/log statement — PASS
  - $logger.on_emit does not include gift_message — PASS
  - `CGI.escapeHTML(gift_message.to_s)` used — PASS (XSS protected)
  - `error do` block unmodified — PASS
  - Email body (Pony mail) never logged — PASS
  - Pony using `:via => :test` — PASS
  - NEW FINDING (from test 3b): Sinatra's default `show_exceptions: true` in dev mode echoes rendered email body in error responses — ACCEPTED AND FIXED by `set :show_exceptions, false` in test setup
- fixes applied: `Sinatra::Application.set :show_exceptions, false` added to test setup

## Cross-cutting review

- reviewers: n/a — T-03 has no proto/API contract changes or cross-service call changes
- verdict: n/a

## Test verification review

- reviewer: test-verification-reviewer
- verdict: conditional → pass (after accepted fixes)
- findings:
  - TV-3 (HIGH): No test asserting gift_message not in span attributes — ACCEPTED AND FIXED (`test_gift_message_not_in_span_attributes`)
  - TV-1 (MEDIUM): No `refute_nil Pony.last_mail` guard — ACCEPTED AND FIXED
  - TV-4/TV-5 (whitespace/multiline): Not in task spec ACs — REJECTED
  - TV-6 (malformed JSON): Pre-existing service behavior, out of T-03 scope — REJECTED
  - TV-7 (tracetest): Deferred per plan to post-T-05 — REJECTED
  - TV-8 (emailMemoryLeak): Out of T-03 scope — REJECTED
- fixes applied: span attribute PII test added, `refute_nil` guard added

## Code quality review

- reviewer: general-purpose (code-quality role)
- verdict: conditional → pass (after accepted fixes)
- findings:
  - WARNING: `test_pony_exception_does_not_capture_gift_message` was vacuous (`pass` + dead `$last_recorded_exception`) — ACCEPTED AND FIXED (real `refute_includes last_response.body` assertion)
  - INFO: Test name `test_nil_order_gift_wrap_handled_gracefully` misleading — ACCEPTED AND FIXED (renamed to `test_no_gift_message_key_handled_gracefully`)
  - INFO: `assert !x.nil?` → `refute_nil x` — ACCEPTED AND FIXED
  - INFO: Ruby version mismatch 3.4.7 vs portable-ruby 3.4.8 — ACCEPTED AND FIXED (`~> 3.4` pessimistic constraint)
  - INFO: Two leading blank lines in Gemfile — REJECTED: cosmetic
  - INFO: Mixed quote styles in Gemfile — REJECTED: pre-existing
  - INFO: Missing blank line in confirmation.erb — REJECTED: cosmetic
- fixes applied: vacuous test fixed, test renamed, `refute_nil`, `~> 3.4` Gemfile constraint

## Docs consistency review

- reviewer: docs-consistency-reviewer
- verdict: conditional → pass
- docs updated: T-03.md `status: todo` → `done`; this evidence file created
- docs intentionally not updated: `docs/ai-knowledge/services/email.md`, `docs/features/checkout-flow/overview.md` — deferred to finalization per plan section 12
- findings:
  - T-03.md status stale — ACCEPTED AND FIXED
  - T-03.evidence.md missing — ACCEPTED (this file)
  - 03-implementation-plan.md T-03 row status `todo` → `done` — ACCEPTED (will update at finalization)
- fixes applied: T-03.md status updated; evidence file created

## Accepted findings

| id | reviewer | change made |
|---|---|---|
| SC-11 | spec-compliance | Added tests 5+6 from task spec + 2 additional (null JSON, span PII guard) |
| SC-10 | spec-compliance | `CGI.escapeHTML(gift_message.to_s)` defensive type coercion in confirmation.erb |
| TV-1 | test-verification | `refute_nil Pony.last_mail` guard in 3 tests |
| TV-3 | test-verification | `test_gift_message_not_in_span_attributes` added |
| F-7.2 | technical-ruby | `ruby "~> 3.4"` added to Gemfile |
| F-8.3 | technical-ruby | `test_gift_message_null_in_json` added |
| F-8.4 / code-quality WARNING | technical-ruby + code-quality | `refute_nil`, `Sinatra::Application.set :show_exceptions, false` |
| Code-quality WARNING | code-quality | `test_pony_exception_does_not_capture_gift_message` fixed to real assertion |
| Code-quality INFO (test name) | code-quality | Renamed to `test_no_gift_message_key_handled_gracefully` |
| Docs T-03.md status | docs-consistency | Updated to `done` |

## Rejected findings with rationale

| id | reviewer | rationale for rejection |
|---|---|---|
| F-1.2 (idiom) | technical-ruby | Task spec explicitly prescribes `nil? \|\| empty?` guard order as a safety requirement; this is intentional, not a style issue |
| F-1.1 (`frozen_string_literal`) | technical-ruby | Pre-existing gap across the entire email service — not T-03's scope to fix |
| F-5.2 (dead alias_method) | technical-ruby | INFO-only; no correctness impact; test file internals |
| TV-4 (whitespace-only) | test-verification | Not in T-03 acceptance criteria |
| TV-5 (multiline) | test-verification | Not in T-03 acceptance criteria |
| TV-6 (malformed JSON) | test-verification | Pre-existing service behavior, out of T-03 scope |
| TV-7 (tracetest) | test-verification | Deferred to post-T-05 per implementation plan section 8 |
| TV-8 (emailMemoryLeak) | test-verification | Out of T-03 scope |

## Remaining risks

- `OpenTelemetry.logger_provider` is not explicitly stubbed in tests; relies on OTel SDK no-op proxy. Safe in current versions but untested assumption.
- Tracetest integration test for email gift_message path deferred — no `test/tracetesting/email/confirmation-with-gift.yaml` yet.
- Pre-existing Sinatra `show_exceptions: true` default in development mode would echo rendered email body (including gift_message) in exception responses. The test now sets `show_exceptions: false`; production deployments already default to false in non-development environments.

## Follow-up tasks

- Tracetest: write `test/tracetesting/email/confirmation-with-gift.yaml` after T-05 completes (verifies gift_message not in spans, route returns 200).
- Finalization: update `docs/ai-knowledge/services/email.md` and `docs/features/checkout-flow/overview.md` to document `gift_message` field and ERB rendering.
