# Task Evidence

## Task

- id: T-04
- title: "Frontend: checkout form gift wrap UI + state threading"
- owner implementer: typescript-frontend-implementer
- status: done

## Files changed

| File | Change |
|---|---|
| `src/frontend/components/CheckoutForm/CheckoutForm.tsx` | Extended `IFormData` with `giftWrap: boolean` and `giftMessage: string` (camelCase); added checkbox + conditional textarea; used `CypressFields` enum for `data-cy` attributes |
| `src/frontend/components/CheckoutForm/GiftMessage.styled.ts` | NEW: `GiftWrapRow` (flex row) and `GiftMessageTextarea` (styled textarea with theme tokens + WCAG focus indicator) |
| `src/frontend/components/CartItems/CartItems.tsx` | Added `giftWrap?: boolean` prop; gift wrap fee row using `S.DataRow` + `ProductPrice`; total useMemo includes +$5 when gift wrap is checked; `currencyCode: selectedCurrency` (post-review fix) |
| `src/frontend/components/Cart/CartDetail.tsx` | Lifted `giftWrap` state with `onGiftWrapChange` callback; passes `giftWrap` to `<CartItems>` and `giftWrap`/`giftMessage` to `placeOrder` |
| `src/frontend/utils/enums/CypressFields.ts` | Added `GiftWrapCheckbox = 'gift-wrap-checkbox'` and `GiftMessageTextarea = 'gift-message-textarea'` |
| `src/frontend/cypress/e2e/Checkout.cy.ts` | Moved intercepts from `before()` to `beforeEach()`; added 5 new gift wrap E2E tests; test 5 adds item, asserts location, submits, and verifies POST body |

## Tests added or updated

| File | Tests |
|---|---|
| `src/frontend/cypress/e2e/Checkout.cy.ts` | `shows gift wrap checkbox on checkout page` |
| | `checking gift wrap shows gift message textarea` |
| | `unchecking gift wrap hides textarea and clears message` |
| | `gift wrap fee row appears in cart summary when gift wrap is checked` |
| | `gift wrap fields included in checkout POST body` |

## Commands run

| Command | Exit code | Output summary |
|---|---|---|
| `npx tsc --noEmit` (src/frontend) | 0 | Clean — no type errors (run twice: before and after all fixes) |

## Implementer self-review

- completeness: All T-04 acceptance criteria met. Gift wrap checkbox + textarea UI, fee row in order summary, total calculation, full TypeScript state threading chain (IFormData → CartDetail → placeOrder → BFF).
- quality: Follows existing codebase patterns. `GiftMessage.styled.ts` uses only Theme token references. Gift wrap row uses same `S.DataRow` + `ProductPrice` pattern as shipping row. State threading uses lift-via-callback, idiomatic for this codebase.
- discipline: TDD ordering: Cypress tests written first, then implementation. TypeScript type check verified.
- testing: Five Cypress tests cover all new UI behavior. Intercepts moved to `beforeEach` for reliability.
- concerns: Gift wrap fee hardcoded as $5 USD — no currency conversion applied (TODO comment added; backend `giftWrapCost: Money` in OrderResult is the correct fix, deferred).

## Spec compliance review

- reviewer: spec-compliance-reviewer
- verdict: pass (13/13 criteria)
- findings: All criteria met. No issues.
- fixes applied: n/a

## Domain expert review

- reviewer: domain-frontend-expert
- verdict: conditional → pass (after fixes)
- findings:
  - **BLOCKER (accepted)**: `currencyCode: 'USD'` hardcoded in gift wrap fee `ProductPrice` — wrong symbol for non-USD users. Fixed to `currencyCode: selectedCurrency`.
  - **BLOCKER-documentation (accepted)**: Hardcoded `5` in total useMemo is always USD units regardless of selected currency. Documented with TODO comment. Full fix requires backend `giftWrapCost: Money` conversion (deferred).
  - **Minor (accepted)**: `IFormData.gift_wrap/gift_message` used snake_case vs camelCase convention. Renamed to `giftWrap`/`giftMessage` throughout.
  - **Minor (accepted)**: `data-cy` raw string literals in CheckoutForm.tsx instead of CypressFields enum values. Fixed to use `CypressFields.GiftWrapCheckbox` and `CypressFields.GiftMessageTextarea`.
  - **Minor (accepted)**: Cypress test 5 missing `cy.location` assertion before gift wrap interaction. Added.
  - **Minor (rejected)**: Two-state desync risk (CartDetail `giftWrap` state + CheckoutForm internal state). Rejected — the callback pattern is intentional and correct; states are synchronized synchronously in the same event handler. Documented with comment.
  - **Pre-existing (rejected)**: `handleChange` stores `string` into `number`-typed fields. Out of T-04 scope.

## Technical review

- reviewer: technical-typescript-reviewer
- verdict: conditional → pass (after fixes)
- findings:
  - **Medium (accepted)**: `data-cy` raw strings in CheckoutForm.tsx — fixed to CypressFields enum values.
  - **Low (accepted)**: snake_case `gift_wrap`/`gift_message` in `IFormData` — renamed to camelCase.
  - **Low (accepted)**: Cypress intercepts in `before()` — moved to `beforeEach()` for test isolation.
  - **Low (rejected)**: `onGiftWrapChange` optionality creates sync hazard. Rejected — the optional pattern is idiomatic React and the current callers always provide the prop. Documented.
  - **Low (rejected)**: `request.body` typed as `any` in Cypress — Cypress API limitation, not an authoring error.
  - **Pre-existing (rejected)**: `handleChange` string-to-number issue, event duck-typing. Out of T-04 scope.

## Observability review

- reviewer: observability-reviewer
- verdict: pass
- findings:
  - No new `console.log`, span attributes, or OTel API calls in any changed file — PASS.
  - `giftMessage` does not appear in any telemetry path — PASS.
  - Existing OTel browser instrumentation (fetch tracer, baggage Proxy in Api.gateway.ts) undisturbed — PASS.
  - Pre-existing: deprecated `SemanticAttributes.HTTP_STATUS_CODE` in `InstrumentationMiddleware.ts` — out of T-04 scope.
- fixes applied: n/a

## Security / data leak review

- reviewer: security-data-leak-reviewer
- verdict: pass
- findings:
  - No `console.log` calls in any changed file — PASS.
  - `giftMessage` not passed to any span attribute in CartDetail or CheckoutForm — PASS.
  - `pages/api/checkout.ts` does not log request body or `giftMessage` — PASS.
  - `giftMessage` not exposed in any error message path — PASS.
  - Low note: Cypress test 5 hardcoded fixture string `'Happy Birthday!'` appears in CI Cypress logs — acceptable (fixture value, not real PII).
- fixes applied: n/a

## Test verification review

- reviewer: test-verification-reviewer
- verdict: conditional → pass (after fixes)
- findings:
  - **Required (accepted)**: `@addToCart` and `@getCart` intercepts in `before()` not reliable for test 5. Moved to `beforeEach()`.
  - **Required (accepted)**: Cypress test 5 missing `cy.location` assertion before gift wrap interaction. Added.
  - **Minor (rejected)**: Test 4 `cy.contains('Gift Wrap')` potentially ambiguous. Rejected — case-sensitive: checkbox label says "Add gift wrap" (lowercase g), fee row says "Gift Wrap" (capital G). No ambiguity.
  - **Minor (rejected)**: Test 1 visits `/cart` with no items. Rejected — `CheckoutForm` renders unconditionally; checkbox always present.
- fixes applied: intercepts moved to `beforeEach`; `cy.location` assertion added to test 5.

## Frontend UI-kit review

- reviewer: frontend-ui-kit-reviewer
- verdict: conditional → pass (after fixes)
- findings:
  - **Medium (accepted)**: `GiftMessageTextarea` focus style `outline: none` without adequate replacement. Added `box-shadow: 0 0 0 2px otelBlue` for WCAG 2.4.11 compliance.
  - **Medium (accepted)**: `currencyCode: 'USD'` in gift wrap fee `ProductPrice`. Fixed to `currencyCode: selectedCurrency`.
  - All theme tokens verified: `borderGray`, `backgroundGray`, `textGray`, `otelBlue`, `mMedium` all exist in `theme.ts`.
  - `GiftMessageTextarea` correctly extends `styled.textarea`.
  - Label associations correct: `htmlFor="gift_wrap"` / `id="gift_wrap"`, `htmlFor="gift_message"` / `id="gift_message"`.
  - `GiftWrapRow` uses flex layout consistent with other single-axis rows.
  - Pre-existing `<label>` column headers in CartItems (no `htmlFor`) — out of T-04 scope.
- fixes applied: focus style `box-shadow` added; currency code fixed.

## Accepted findings (summary)

| id | reviewer | change made |
|---|---|---|
| F-CURRENCY-DISPLAY (blocker) | domain-frontend-expert, ui-kit | `currencyCode: 'USD'` → `currencyCode: selectedCurrency` in CartItems.tsx gift wrap fee row |
| F-TODO-HARDCODED-5 | domain-frontend-expert | Added TODO comment documenting hardcoded $5 limitation and deferred fix |
| F-SNAKE-CASE | domain-frontend-expert, technical-typescript | Renamed `gift_wrap`/`gift_message` → `giftWrap`/`giftMessage` in IFormData and all usages |
| F-DATA-CY-ENUM | domain-frontend-expert, technical-typescript | `data-cy="gift-wrap-checkbox"` → `data-cy={CypressFields.GiftWrapCheckbox}` and `data-cy="gift-message-textarea"` → `data-cy={CypressFields.GiftMessageTextarea}` |
| F-INTERCEPT-BEFORE | test-verification-reviewer, technical-typescript | Moved `cy.intercept` calls from `before()` to `beforeEach()` |
| F-CYPRESS-LOCATION | domain-frontend-expert | Added `cy.location('href').should('match', /\/cart$/)` in test 5 before gift wrap interaction |
| F-FOCUS-A11Y | frontend-ui-kit-reviewer | Added `box-shadow: 0 0 0 2px otelBlue` to `GiftMessageTextarea` focus style |

## Rejected findings with rationale

| id | reviewer | rationale for rejection |
|---|---|---|
| F-STATE-DESYNC | domain-frontend-expert | Callback pattern is correct; both states synchronized synchronously in same event handler. Documented. |
| F-OPTIONAL-CALLBACK | technical-typescript-reviewer | Optional pattern is idiomatic React; breaking change to make required; all current callers provide the prop. |
| F-TEST4-AMBIGUOUS | test-verification-reviewer | `cy.contains('Gift Wrap')` is case-sensitive. Checkbox label uses "Add gift wrap" (lowercase), fee row uses "Gift Wrap" (capital). No ambiguity. |
| F-TEST1-EMPTY-CART | test-verification-reviewer | CheckoutForm renders unconditionally; gift wrap checkbox always present on `/cart`. |
| F-REQUEST-BODY-ANY | technical-typescript-reviewer | Cypress API limitation; `Interception<TRequest>` defaults to `any`. Runtime assertions are correct. |
| F-HANDLECHANGE-STRINGS | multiple | Pre-existing issue, not introduced by T-04. |
| F-HTTP-STATUS-CODE | observability-reviewer | Pre-existing deprecated semantic convention in InstrumentationMiddleware.ts. Out of T-04 scope. |

## Remaining risks

- Gift wrap fee displayed as $5 regardless of selected currency (no conversion). The checkout backend's `giftWrapCost: Money` in `OrderResult` (set in T-02) carries the correctly-converted value. A follow-up task should read this value from the order confirmation response and use it for display.
- No Jest/React Testing Library unit tests — the project does not have RTL configured. Cypress E2E tests are the only frontend test coverage.
- Cypress E2E tests require a running stack to execute; not run locally in CI-equivalent mode.

## Follow-up tasks

- Read `OrderResult.giftWrapCost` on the confirmation page (T-05) and use that value instead of the hardcoded $5 in the CartItems summary.
- Add `data-cy` attribute to the gift wrap fee row element for more precise Cypress targeting.
