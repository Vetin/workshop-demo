# Task Evidence

## Task

- id: T-02
- title: Implement gift wrap logic in checkout service (Go)
- owner implementer: go-implementer
- status: done

## Files changed

| File | Change |
|---|---|
| `src/checkout/main.go` | Added gift wrap logic: early span attribute, currency conversion, fee addition to total, span event + amount attribute, OrderResult population, call-site sanitization, updated `sendOrderConfirmation` signature with `giftMessage string` parameter |
| `src/checkout/main_test.go` | New file: 6 unit tests covering `sendOrderConfirmation`, span attributes, PII guard, and money formula |

## Tests added or updated

| File | Tests |
|---|---|
| `src/checkout/main_test.go` | `TestSendOrderConfirmation_GiftMessageOmittedWhenWrapFalse` |
| | `TestSendOrderConfirmation_GiftMessageIncludedWhenWrapTrue` |
| | `TestGiftWrapSpanAttribute_AlwaysSet` (table: gift_wrap=true, gift_wrap=false) |
| | `TestGiftWrapSpanAttribute_GiftMessageNotPresent` |
| | `TestMoneyToFloat_Formula` |
| | `TestSendOrderConfirmation_SignatureAcceptsGiftMessage` (compile guard) |

## Commands run

| Command | Exit code | Output summary |
|---|---|---|
| `go build ./...` (src/checkout) | 0 | Clean build |
| `go test ./... -v -run "TestSendOrderConfirmation\|TestGiftWrap\|TestMoneyToFloat"` | 0 | 6/6 tests pass |

## Implementer self-review

- completeness: All T-02 acceptance criteria met. Gift wrap logic is in PlaceOrder, span attributes set, OrderResult populated, sendOrderConfirmation updated, giftMessage passed only when gift_wrap=true.
- quality: Follows existing code patterns (money.Must/Sum, otelhttp.Post, status.Errorf). Error wrapping uses %w. Span attributes use app.* prefix per project convention.
- discipline: TDD followed — test file written alongside implementation. Tests verified against recorder spans.
- testing: All new behavior covered by unit tests. `gift_message` PII guard verified by TestGiftWrapSpanAttribute_GiftMessageNotPresent.
- concerns: Pre-existing `app.shipping.amount` and `app.order.amount` formulas at lines 392-393 still use the wrong divisor (1000000000 instead of 10000000). Out of T-02 scope; filed as follow-up.

## Spec compliance review

- reviewer: spec-compliance-reviewer
- verdict: conditional → pass (after orchestrator-applied fixes)
- findings:
  - SC-01 (span attribute `app.order.gift_wrap` before chargeCard): PASS
  - SC-02 (`app.order.gift_wrap.amount` only when gift_wrap=true): PASS
  - SC-03 (`gift_wrap_fee_applied` event): PASS
  - SC-04 (OrderResult.GiftWrap/GiftWrapCost populated): PASS
  - SC-05 (giftMessage only passed to email when gift_wrap=true): PASS
  - SC-06 (giftMessage not in any span attribute/event): PASS
  - SC-07 (`sendOrderConfirmation` accepts giftMessage parameter): PASS
  - SC-08 (`otelhttp.Post` preserved): PASS
  - SC-09 (GiftWrapCost=nil when gift_wrap=false): PASS
  - SC-10 (fee converted to user currency): PASS
  - SC-11/SC-12 (go build/test confirmed): PASS (exit 0 locally)
- fixes applied: n/a (all passing after orchestrator applied F-02, F-03, F-01-formula fixes)

## Domain expert review

- reviewers: domain-checkout-expert
- verdict: conditional → pass (after fixes)
- findings:
  - F-01 (BLOCKER): `GetNanos()/1000000000` integer division always gives 0 — confirmed by failing TestMoneyToFloat_Formula. Formula must use divisor 10_000_000 (10^7). ACCEPTED AND FIXED.
  - F-02 (minor): `convertedFee, err :=` shadows outer `var err error` at line 305, breaking deferred span error event for gift wrap conversion failures. ACCEPTED AND FIXED.
  - F-03 (minor): `return err` in sendOrderConfirmation is always nil at that point. ACCEPTED AND FIXED to `return nil`.
- fixes applied:
  - F-01: Changed gift wrap amount formula divisor from `1000000000` to `10_000_000` in both `main.go` and `main_test.go`
  - F-02: Changed `convertedFee, err :=` to `var convertedFee *pb.Money; convertedFee, err =` to avoid shadowing outer err
  - F-03: Changed `return err` to `return nil` in `sendOrderConfirmation`

## Technical review

- reviewers: technical-go-reviewer
- verdict: pass
- findings: No blocking findings. Implementation idiomatically correct. Error handling follows Go conventions. otelhttp.Post preserved for trace context.
- fixes applied: n/a

## Observability review

- reviewer: observability-reviewer
- verdict: conditional → pass (after F-01 formula fix)
- findings:
  - Same F-01 nanos formula issue (INFO-level from observability perspective; confirmed blocking by test failure)
  - `app.order.gift_wrap` naming correct per app.* convention
  - `app.order.gift_wrap.amount` only set when gift_wrap=true — correct
  - `gift_wrap_fee_applied` event name follows snake_case for workshop pedagogy
  - All trace context propagation via otelhttp.Post preserved
- fixes applied: F-01 formula fix resolves the finding

## Security / data leak review

- reviewer: security-data-leak-reviewer
- verdict: pass
- findings:
  - `gift_message` / `giftMessage` not present in any span attribute, span event, or log body — PASS
  - `emailPayload` not logged at any level — PASS
  - BFF enforcement: `sendOrderConfirmation` only receives giftMessage when gift_wrap=true — PASS
  - `req` (PlaceOrderRequest) not logged directly — PASS (logger logs only specific safe fields)
- fixes applied: n/a

## Cross-cutting review

- reviewers: distributed-flow-reviewer
- verdict: conditional → pass
- findings:
  - DF-01: otelhttp.Post preserved for trace context propagation to email service — PASS
  - DF-02: Currency conversion reuses existing convertCurrency helper — PASS
  - DF-03: Email service Ruby template not yet updated to render gift_message — NOTED, deferred to T-03 (correct by design)
- fixes applied: DF-03 deferred to T-03

## Test verification review

- reviewer: test-verification-reviewer
- verdict: conditional → pass
- findings:
  - TV-01: sendOrderConfirmation payload tests present — PASS
  - TV-02: span attribute tests present — PASS
  - TV-03: PII guard test present — PASS
  - TV-04: span attribute tests use recording span (tracetest.SpanRecorder), not live service — NOTED as adequate for unit test scope; trace integration test (test/tracetesting/) deferred to T-02 follow-up
- fixes applied: n/a

## Code quality review

- reviewer: general-purpose (code-quality role)
- verdict: pass
- findings: No blocking quality issues. Code follows existing patterns. Comments appropriate. No dead code.
- fixes applied: n/a

## Docs consistency review

- reviewer: docs-consistency-reviewer
- verdict: conditional pass → pass
- docs updated: none (T-02 doc updates deferred to finalization per plan)
- docs intentionally not updated: all finalization docs scheduled for `/sdd-finalize` step
- findings:
  - T-02.md status updated to `done` (this evidence file created)
- fixes applied: T-02.md status updated

## Accepted findings

| id | reviewer | change made |
|---|---|---|
| F-01 (domain-checkout) | domain-checkout-expert | Nanos formula divisor fixed: `1000000000` → `10_000_000` in main.go and main_test.go |
| F-02 (domain-checkout) | domain-checkout-expert | `convertedFee, err :=` → separate var declaration to avoid shadowing outer err |
| F-03 (domain-checkout) | domain-checkout-expert | `return err` → `return nil` in sendOrderConfirmation |
| Pre-existing vet issue | orchestrator | `status.Errorf(codes.Internal, err.Error())` → `status.Errorf(codes.Internal, "%s", err.Error())` at line 319 (pre-existing go vet finding blocking `go test`) |

## Rejected findings with rationale

| id | reviewer | rationale for rejection |
|---|---|---|
| DF-03 (distributed-flow) | distributed-flow-reviewer | Email template not yet updated — T-03 is explicitly scoped to that. Not a T-02 defect. |
| TV-04 (test-verification) | test-verification-reviewer | Span attribute tests use SpanRecorder (unit test) not live tracetesting infra. Adequate for T-02 scope. Trace integration test is a T-02 follow-up. |

## Remaining risks

- Pre-existing `app.shipping.amount` and `app.order.amount` formulas at lines 392-393 in main.go still use `/1000000000` divisor (wrong for non-zero nanos). These were not introduced by T-02 and are out of scope.
- Trace integration test (`test/tracetesting/checkout/checkout_gift_wrap.yaml`) not yet written — deferred to follow-up task after T-05.

## Follow-up tasks

- Trace integration test: write `test/tracetesting/checkout/checkout_gift_wrap.yaml` and update `test/tracetesting/all.yaml` after T-05 completes.
- Technical debt: fix pre-existing `/1000000000` divisor in `app.shipping.amount` and `app.order.amount` span attributes (separate change, not gift-wrap-checkout scope).
