# Plan Review: Gift Wrap at Checkout

- **Feature area**: checkout-flow
- **Change slug**: gift-wrap-checkout
- **Plan doc**: [03-implementation-plan.md](03-implementation-plan.md)
- **Reviewers**: 13 agents (see below)
- **Plan status**: **approved** (after applying findings below)

---

## Reviewers dispatched

| Reviewer | Focus |
|---|---|
| service-contract-reviewer | Proto field numbers, stub regeneration completeness, HTTP contract |
| distributed-flow-reviewer | Cross-service call ordering, trace context, Kafka shape |
| observability-reviewer | Span attribute names, formula, placement, event naming |
| security-data-leak-reviewer | PII handling, log surfaces, exception capture risks |
| test-verification-reviewer | Test infrastructure existence, AC coverage, trace test placement |
| domain-checkout-expert | Go code patterns, insertation point, OrderResult construction |
| domain-email-expert | Ruby send_email signature, ERB template, test infrastructure |
| domain-frontend-expert | CartItems component, CartDetail pattern, TypeScript layers |
| technical-go-reviewer | money.Must, error wrapping, nil Money, float formula |
| technical-ruby-reviewer | OpenStruct nil, send_email signature, CGI require |
| technical-typescript-reviewer | Proto camelCase, CartDetail callback, useMemo pattern |
| frontend-ui-kit-reviewer | Theme tokens, CheckoutItem wrong component, font-size |
| docs-consistency-reviewer | Design vs. plan contradictions, missing finalization docs |

---

## Finding index and verdicts

| ID | Reviewer(s) | Severity | Topic | Verdict |
|---|---|---|---|---|
| F-01 | service-contract, docs-consistency, domain-checkout, technical-go | BLOCKER | `sendOrderConfirmation` parameter order contradicts between 01-design.md and T-02 | **ACCEPT** — canonical order is `(ctx, email, order, giftMessage)` per T-02; fix 01-design.md |
| F-02 | distributed-flow, technical-go, domain-checkout | BLOCKER | `giftWrapCostLocalized` var declared inside `if` block; out of scope at OrderResult construction and span attributes | **ACCEPT** — must declare at function scope before `if req.GiftWrap` |
| F-03 | observability, technical-go | BLOCKER | Float formula for `app.order.gift_wrap.amount` differs from existing `app.shipping.amount` formula in main.go | **ACCEPT** — use `strconv.ParseFloat(fmt.Sprintf("%d.%02d", Units, Nanos/1000000000), 64)` |
| F-04 | observability | BLOCKER | `app.order.gift_wrap` (bool) set after `chargeCard`; absent on failed charge spans that have the `gift_wrap_fee_applied` event | **ACCEPT** — set the bool attribute early (with user_id/user_currency) before chargeCard |
| F-05 | domain-email, technical-ruby | BLOCKER | T-03 `send_email` "Before" signature wrong: actual is `def send_email(data)` not `def send_email(recipient, order)` | **ACCEPT** — simplest fix: keep `data` arg, add `data.gift_message` inside the function; no signature change |
| F-06 | domain-email, technical-ruby, security | BLOCKER | `require 'cgi'` described as optional; it is mandatory | **ACCEPT** — change to explicit mandatory step in T-03 |
| F-07 | domain-frontend, frontend-ui-kit | BLOCKER | `CheckoutItem` wrong component for CartItems fee row; correct pattern is `S.DataRow` + `ProductPrice` | **ACCEPT** — fix T-04 Section 4 code example |
| F-08 | domain-frontend, technical-typescript | BLOCKER | T-04 Section 5 describes `CartDetail.onPlaceOrder` as a prop callback; it is an internal `useCallback`, not a prop | **ACCEPT** — fix T-04 Section 5 |
| F-09 | technical-typescript | BLOCKER | T-05 `query.order?.gift_wrap` accesses Next.js router string as object; always `undefined` at runtime | **ACCEPT** — use destructuring from `JSON.parse(...)` result at line 21 |
| F-10 | technical-typescript, domain-frontend | BLOCKER | T-05 uses snake_case (`gift_wrap`, `gift_wrap_cost`) throughout; TypeScript types will be camelCase | **ACCEPT** — replace all with `giftWrap`, `giftWrapCost` |
| F-11 | technical-typescript | BLOCKER | T-05 references non-existent `CostDisplay` function; must use raw units/nanos arithmetic | **ACCEPT** — fix T-05 Section 1 |
| F-12 | service-contract | WARNING | `react-native-app` stubs not regenerated by `docker-gen-proto.sh`; T-01 implies automatic but it is not | **ACCEPT** — add explicit manual step to T-01 |
| F-13 | service-contract, docs-consistency | WARNING | `reserved 4` comment in design says "previously used"; actual proto has no field 4 ever defined | **ACCEPT** — correct 01-design.md comment to "previously unused" |
| F-14 | technical-go | WARNING | `money.Must(money.Sum(...))` panics on mismatch; plan comment acknowledges risk but keeps `money.Must` | **REJECT as blocker — ACCEPT as WARNING**: existing code uses identical pattern for shipping/items; the CurrencyCode is explicitly set per A-11; keeping `money.Must` is intentional and consistent. Add a warning comment in T-02. |
| F-15 | technical-go | WARNING | Plan comment claims `omitempty` handles nil `*pb.Money` with `encoding/json`; it does not (nil = `null` not omitted) | **ACCEPT** — fix T-02 comment; add note that email service must tolerate `"gift_wrap_cost": null` |
| F-16 | domain-checkout | WARNING | T-02 OrderResult snippet shows `uuid.New().String()` instead of existing `orderID` variable | **ACCEPT** — fix snippet |
| F-17 | observability, domain-checkout, technical-go | SUGGESTION | T-02 uses `cs *checkoutService` receiver type; actual struct is `*checkout` | **ACCEPT** — fix all occurrences |
| F-18 | observability | WARNING | Span event `gift_wrap_fee_applied` is snake_case; existing events are lowercase single-word verbs | **REJECT rename**: The event name is descriptive and pedagogically useful for the workshop. Accept suggestion to add a code comment noting the intentional deviation. |
| F-19 | observability | WARNING | `app.order.gift_wrap=false` always set has no precedent | **REJECT as blocker**: This is an intentional design choice for AC-08/09. Accept suggestion to add inline comment. |
| F-20 | security | BLOCKER | No prohibition on logging `emailPayload` byte slice in `sendOrderConfirmation` | **ACCEPT** — add to T-02 "What must NOT change" |
| F-21 | security | WARNING | BFF not listed in sensitive data enforcement points (Section 7 of plan) | **ACCEPT** — add to plan Section 7 |
| F-22 | security | WARNING | Pony exception may embed rendered email body; `record_exception` can capture it | **ACCEPT as WARNING**: Add test requirement to T-03 acceptance criteria (verify `exception.message` doesn't contain gift_message on mail failure). |
| F-23 | test-verification | BLOCKER | `src/checkout/main_test.go` does not exist; no OTel test span recorder available | **ACCEPT** — T-02 must describe test harness setup (interface for injectable clients or tracetest-based approach) |
| F-24 | test-verification, domain-email | BLOCKER | `src/email/Gemfile` has no test gems; no test infrastructure | **ACCEPT** — T-03 must include Gemfile update step |
| F-25 | test-verification | BLOCKER | `src/frontend/package.json` has no Jest/RTL; `npm test` script absent | **ACCEPT** — redirect T-04/T-05 frontend unit tests to Cypress; Jest/RTL setup is out of scope for this change |
| F-26 | test-verification | BLOCKER | AC-09 (`app.order.gift_wrap=false`) has no automated test | **ACCEPT** — plan must note that `test/tracetesting/checkout/place-order.yaml` needs a new assertion |
| F-27 | test-verification | WARNING | New trace test in wrong directory; `test/tracetesting/checkout_gift_wrap.yaml` won't run | **ACCEPT** — move to `test/tracetesting/checkout/` and update `all.yaml` |
| F-28 | distributed-flow | WARNING | Parallel T-02/T-03 have no machine-checkable JSON contract; both must self-verify against design | **ACCEPT** — add note to plan Section 11 |
| F-29 | distributed-flow | SUGGESTION | T-02 should explicitly preserve `otelhttp.Post` call | **ACCEPT** — add to T-02 |
| F-30 | docs-consistency | BLOCKER | `docs/ai-knowledge/services/email.md` not in finalization docs list | **ACCEPT** — add to plan |
| F-31 | docs-consistency | BLOCKER | `docs/ai-knowledge/services/checkout.md` not in finalization docs list | **ACCEPT** — add to plan |
| F-32 | docs-consistency | BLOCKER | `docs/ai-knowledge/communication/http-map.md` not in finalization docs list | **ACCEPT** — add to plan |
| F-33 | docs-consistency | WARNING | Risky flow #4 in `communication/overview.md` says field changes require atomic redeploy; contradicts plan's backward-compat claim | **ACCEPT** — add to finalization docs list |
| F-34 | docs-consistency | WARNING | `gift_wrap_fee_applied` span event not in finalization update list for checkout overview | **ACCEPT** — add to plan Section 12 |
| F-35 | docs-consistency | WARNING | Finalization list omits source paths update (CheckoutForm.tsx, CartItems.tsx, GiftMessage.styled.ts) | **ACCEPT** — add to plan Section 12 |
| F-36 | docs-consistency, observability | WARNING | `gift_message` sensitive data rule not in `observability/overview.md` finalization update | **ACCEPT** — add to plan Section 12 |
| F-37 | domain-frontend | WARNING | CartItems `total` useMemo will show $5 short when gift wrap selected | **ACCEPT** — T-04 must address: update CartItems total to include gift wrap fee |
| F-38 | technical-typescript | WARNING | `ApiGateway.placeOrder` uses `body: order` spread, not `JSON.stringify`; new fields auto-included after T-01 | **ACCEPT** — fix T-04 Section 6 |
| F-39 | technical-typescript | WARNING | BFF uses `body as PlaceOrderRequest` passthrough; new fields auto-included; no destructuring needed | **ACCEPT** — fix T-04 Section 7 |
| F-40 | technical-typescript | WARNING | `IProductCheckout` extends `OrderResult`; no manual Cart.ts update needed | **ACCEPT** — fix T-05 Section 3 |
| F-41 | technical-typescript | WARNING | `useMemo` deps array in T-05 example uses `[query.order]`; current code uses destructured vars | **ACCEPT** — fix T-05 to match existing destructure pattern |
| F-42 | frontend-ui-kit | WARNING | `font-size: 14px` hardcoded; should use `theme.sizes.mMedium` | **ACCEPT** — fix T-04 code snippet |
| F-43 | frontend-ui-kit | WARNING | Unstyled `<input type="checkbox">` visually inconsistent with rest of form | **ACCEPT** — add `GiftWrapRow` styled wrapper suggestion to T-04 |
| F-44 | technical-go | WARNING | `%w` vs `%+v` inconsistency in error wrapping | **REJECT**: `%w` is more correct Go idiom; keeping it is the right call. No change needed. |
| F-45 | technical-ruby | SUGGESTION | Non-string `gift_message` would cause `TypeError` in `CGI.escapeHTML` | **ACCEPT** — add `.to_s` note to T-03 |
| F-46 | domain-frontend | BLOCKER | Five TypeScript layers, not four; `Cart.provider.tsx` is a pass-through | **ACCEPT** — add Cart.provider.tsx to T-04 |

---

## Changes applied to plan and task files

All accepted findings were applied to the relevant files immediately after this review. See commit history for specific diffs.

### Files updated

| File | Findings applied |
|---|---|
| `01-design.md` | F-01 (parameter order), F-13 (reserved 4 comment) |
| `03-implementation-plan.md` | F-21, F-26, F-27, F-28, F-30–F-36 |
| `tasks/T-01.md` | F-12 (react-native-app manual step) |
| `tasks/T-02.md` | F-01, F-02, F-03, F-04, F-14 (warning), F-15, F-16, F-17, F-19, F-20, F-29 |
| `tasks/T-03.md` | F-05, F-06, F-22, F-24, F-45 |
| `tasks/T-04.md` | F-07, F-08, F-37, F-38, F-39, F-42, F-43, F-46 |
| `tasks/T-05.md` | F-09, F-10, F-11, F-40, F-41 |

---

## Rejected findings with rationale

| ID | Finding | Rationale |
|---|---|---|
| F-14 | `money.Must` → explicit error return | Existing code uses `money.Must` for shipping/items in identical pattern. A-11 ensures CurrencyCode is set correctly. Breaking the pattern here creates inconsistency. Accepted as warning with a note. |
| F-18 | Rename `gift_wrap_fee_applied` event | Design explicitly chose this name. The snake_case is more descriptive for workshop pedagogy. Added inline code comment noting intentional deviation. |
| F-19 | Don't always-set `app.order.gift_wrap=false` | Required by AC-08 and AC-09. Intentional design decision. Added inline code comment. |
| F-44 | Use `%+v` instead of `%w` for error wrapping | `%w` is the correct modern Go idiom for error wrapping. Inconsistency with existing codebase is a pre-existing issue to improve, not regress from. |

---

## Unresolved questions

None. All findings resolved without user escalation required.

---

## Plan status

**Approved.** All blocker and warning findings have been applied to the plan and task files. The plan is ready for execution.
